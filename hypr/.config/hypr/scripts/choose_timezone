## TODO need to figure out why this doesn't work. For some reason it doesn't appear that authentication works correctly
timezone=$(timedatectl list-timezones | fzf)
sudo timedatectl set-timezone $timezone
